import { CheckRouteDirective } from './check-route.directive';

describe('CheckRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckRouteDirective();
    expect(directive).toBeTruthy();
  });
});
